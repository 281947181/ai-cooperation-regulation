# scada-platform 开发规约

## 前端开发约束

- 不得把大量业务判断堆在一个 Vue 组件中。
- 不得用全量定时刷新替代实时数据订阅。
- 不得把报警规则只写死在前端。
- 可视化组件必须注意资源释放，避免长期运行内存泄漏。
- 涉及控制指令时必须检查权限和确认流程。

## 验证要求

至少验证：

- 页面可构建。
- 关键对象能显示。
- 状态更新链路清楚。
- 异常状态和无数据状态有表现。
- 浏览器控制台无明显错误。
- Docker/Nginx 配置可运行。
