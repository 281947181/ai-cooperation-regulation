# plm_william 架构规约

## 当前判断

该仓库与 PLM / 研发销售业务系统相关。具体架构、框架版本和模块划分必须以后续读取代码为准。

## 架构原则

- 不在未读取代码前假设具体框架版本。
- 如果本仓库为后端，应重点确认 Controller、Service、DAO/Mapper、Entity/DTO/VO 边界。
- 如果本仓库为历史版本，应谨慎与 `plm` 仓库区分，不得直接混用规则。
- 不得在未确认依赖和启动方式前修改部署逻辑。

## 待确认

```text
仓库职责：后端 / 前端 / 全栈 / 历史版本
主要框架：
数据库：
主要模块：
部署方式：
与 plm 仓库关系：
```
