# dev-platform 开发规约

## 基本要求

本项目开发必须遵守：

```text
constitution.md
runtime.md
projects/dev-platform/README.md
```

## 远程协作

该仓库默认分支为 `main`。每轮任务必须以远程 `main` 最新状态为基线，完成后默认提交并推送远程。

## 开发约束

- 先确认仓库职责，再修改实现。
- 如涉及脚本、CI/CD、Docker 或工具链，必须避免破坏已有开发环境。
- 工具类能力必须有清晰输入、输出和错误处理。
- 不得把一次性脚本伪装成长期平台能力。

## 待补充

```text
构建命令：
启动命令：
测试命令：
主要目录：
关键配置：
```
