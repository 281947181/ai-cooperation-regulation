# plm_frontend_william 架构规约

## 当前判断

该仓库为 PLM 方向前端仓库。具体技术栈需读取代码确认。

## 前端架构原则

- 页面、API、路由、状态管理应分层清晰。
- 不得把复杂业务规则全部堆在页面组件中。
- 权限控制不能只依赖前端隐藏按钮。
- 字段、接口和后端返回结构必须保持一致。
- 与后端仓库关系必须明确，不得混用接口约定。

## 待确认

```text
前端框架：
构建工具：
UI 组件库：
状态管理：
API 目录：
路由目录：
部署方式：
关联后端仓库：
```
