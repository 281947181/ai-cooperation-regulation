# plm_frontend_william 业务规约

## 业务方向

该仓库承载 PLM / 研发销售业务系统相关前端页面。具体页面和业务模块需读取代码确认。

## 前端业务原则

- 页面展示必须服务于明确业务对象。
- 表单字段必须与后端接口契约一致。
- 状态展示不能只依赖前端临时枚举，必须与后端业务状态一致。
- 权限展示与后端权限校验必须保持一致。

## 待确认

```text
主要页面：
主要业务对象：
接口模块：
权限标识：
菜单结构：
```
