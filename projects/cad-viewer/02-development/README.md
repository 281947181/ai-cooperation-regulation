# cad-viewer 开发规约

## Codex 约束

- 不得承诺前端直接解析 CATIA 原生文件。
- 不得把 CAD 转换逻辑混入普通页面组件。
- 不得把大模型直接打包进前端构建产物。
- 不得忽略模型加载失败、超时、格式不兼容等异常状态。
- 不得为了效果引入过重依赖而不说明体积和维护成本。

## 验证要求

至少验证：

- 示例 GLB 模型可加载。
- 旋转、缩放、平移正常。
- 加载中、加载失败、空模型状态有处理。
- 前端构建通过。
- 模型资源路径可配置。
